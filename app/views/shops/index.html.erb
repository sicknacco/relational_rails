<h1>Shops Index Page</h1>

<ul>
  <% @shops.each do |shop| %>
  <li><%= shop.name %> <%= button_to "Update #{@shop.name}", "/shops/#{@shop.id}/edit" %></li>
  <% end %>
</ul>

<%= link_to "New Shop", "/shops/new" %>